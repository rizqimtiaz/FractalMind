# Required: OpenAI API key used by the Vercel AI SDK in /app/api/* routes.
# Create one at https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Optional: override the default model. Defaults to "gpt-4o-mini".
# OPENAI_MODEL=gpt-4o
